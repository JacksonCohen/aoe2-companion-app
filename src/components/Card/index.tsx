import React from 'react';

interface Props {
  source: string;
  name: string;
}

const Card = ({ source, name }: Props) => {
  return <div className='card__container'></div>;
};

export default Card;
