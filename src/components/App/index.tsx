import React, { Fragment } from 'react';
import Randomizer from '../Randomizer';

import '../../../sass/style.scss';
import './_App.scss';

const App = () => {
  return (
    <Fragment>
      <Randomizer />
    </Fragment>
  );
};

export default App;
